**Badge** — tinted status pill. `tone` sets the colour; text is auto-uppercased.

```jsx
<Badge tone="success">Live</Badge>
<Badge tone="info">New</Badge>
<Badge tone="error">Hot</Badge>
```

Tones: `success` `info` `error` `gold` `muted`. Background is always a 12% tint of the tone colour.
