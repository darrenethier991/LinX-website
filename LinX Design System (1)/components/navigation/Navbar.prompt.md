**Navbar** — the sticky marketing top bar. Composes `Button` for the CTA.

```jsx
<Navbar
  links={["How It Works", "Pricing", "FAQ", "Contact"]}
  cta="Get Started Free"
/>
```

Links accept strings or `{label, href}`. Sits directly on the black page background.
